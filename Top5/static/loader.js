document.addEventListener("DOMContentLoaded", function() {
    // Sayfa yüklendiğinde loading-container'ı gizle
    var loadingContainer = document.getElementById("loading-container");
    if (loadingContainer) {
      loadingContainer.style.display = "none";
    }
  });
  